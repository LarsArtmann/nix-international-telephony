# Features

> Honest inventory of what this project does, by status. Generated by studying
> the actual code, not the marketing claims. Updated as features ship, change,
> or break.

## Status legend

| Status                    | Meaning                                                      |
| ------------------------- | ------------------------------------------------------------ |
| 🟢 `FULLY_FUNCTIONAL`     | Works as intended, exercised by tests or daily use.          |
| 🟡 `PARTIALLY_FUNCTIONAL` | Ships but has known gaps, edge-case bugs, or missing pieces. |
| 🔴 `BROKEN`               | Present in code but not working / disabled / failing.        |
| ⚪ `PLANNED`              | Designed or documented but **not yet implemented** in code.  |

> A feature earns `FULLY_FUNCTIONAL` only when you can point to the code that
> delivers it AND confirm it works. If you're unsure, it's `PARTIALLY_FUNCTIONAL`
> at best, never round up.

## NixOS module (`services.telephony`)

| Feature                                                        | Status                    | Notes                                                                                                                                                            |
| -------------------------------------------------------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Declarative option schema with assertions                       | 🟢 `FULLY_FUNCTIONAL`     | All options typed + described; 8 assertions (`modules/telephony.nix:355-400`); `nix flake check` green                                                            |
| FreeSWITCH wiring via generated `configDir`                     | 🟢 `FULLY_FUNCTIONAL`     | `modules/telephony.nix:402-405`; VM test boots the full stack (`tests/pbx.nix:47`)                                                                                |
| Event socket (`fs_cli`, localhost-only, configured password)    | 🟢 `FULLY_FUNCTIONAL`     | `modules/freeswitch.nix:213-222`; every VM test assertion drives it                                                                                               |
| Echo test extension `9196`                                     | 🟢 `FULLY_FUNCTIONAL`     | `modules/freeswitch.nix:367-372`; real `originate loopback/9196` call answered (`tests/pbx.nix:68-69`)                                                            |
| Self-signed TLS bootstrap (per-host cert, nginx-compatible key) | 🟢 `FULLY_FUNCTIONAL`     | `modules/telephony.nix:415-445`; exercised by the VM test (nginx serves HTTPS with the generated cert)                                                            |
| `config.js` generation (SIP domain + TURN ICE servers)          | 🟢 `FULLY_FUNCTIONAL`     | `modules/telephony.nix:170-183`; content asserted over HTTPS (`tests/pbx.nix:78-79`)                                                                              |
| Firewall management (SIP/RTP/HTTPS/TURN)                       | 🟢 `FULLY_FUNCTIONAL`     | `modules/telephony.nix:477-491`; ports reachable in the VM test with `firewall.enable = true`                                                                     |
| Extensions (SIP users: password, display name, toll allow, VM)  | 🟡 `PARTIALLY_FUNCTIONAL` | Directory generated + password verified via `user_data` (`tests/pbx.nix:63-64`), but no SIP REGISTER or registered-endpoint call is tested                        |
| Ring groups (simultaneous ring, timeout, voicemail fallback)    | 🟡 `PARTIALLY_FUNCTIONAL` | Dialplan generated (`modules/freeswitch.nix:94-110`); no test dials a group and the voicemail fallback path is untested                                            |
| ITSP gateway (outbound E.164, toll_allow gate, DID inbound)     | 🟡 `PARTIALLY_FUNCTIONAL` | Dialplan generated (`modules/freeswitch.nix:126-151`, `399-405`); never validated against a real provider; demo/test configs run with `gateway = null`             |
| PSTN denial paths (403 without toll allow, 503 without gateway) | 🟡 `PARTIALLY_FUNCTIONAL` | Generated (`modules/freeswitch.nix:130-151`); not asserted by any test                                                                                             |
| Voicemail (per-extension boxes, `*98` check)                    | 🟡 `PARTIALLY_FUNCTIONAL` | Dialplan entry generated (`modules/freeswitch.nix:374-379`); no test exercises voicemail; prompts depend on the sounds package                                      |
| Call recording (`record_session` WAV)                           | 🟡 `PARTIALLY_FUNCTIONAL` | `modules/freeswitch.nix:49-55`; recordings dir asserted (`tests/pbx.nix:90`) but no test verifies a file appears after a call                                       |
| NAT advertisement (`natAddress`)                               | 🟡 `PARTIALLY_FUNCTIONAL` | Wired into SDP/SIP vars (`modules/freeswitch.nix:44`, `175-176`); untested behind real NAT                                                                        |
| RTP port range options                                         | 🟡 `PARTIALLY_FUNCTIONAL` | Applied to `switch.conf.xml` + firewall (`modules/freeswitch.nix:233-234`, `modules/telephony.nix:490`); range never asserted                                      |
| Manual TLS mode (external cert/key paths)                       | 🟡 `PARTIALLY_FUNCTIONAL` | Option + wiring exist (`modules/telephony.nix:341-350`, `165-166`); never tested; the README ACME example is unexercised                                            |
| coturn STUN/TURN wiring                                        | 🟡 `PARTIALLY_FUNCTIONAL` | Unit + port 3478 asserted (`tests/pbx.nix:86-87`); TURN relay allocation never tested; static credentials live in the served `config.js`                          |
| aarch64-linux outputs                                          | 🟡 `PARTIALLY_FUNCTIONAL` | Declared (`flake.nix:29-32`); only ever evaluated — `nix flake check` on x86_64 skips them                                                                        |

## Web client

| Feature                                              | Status                    | Notes                                                                                                                                    |
| ---------------------------------------------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| HTTPS webphone serving (static site over TLS)        | 🟢 `FULLY_FUNCTIONAL`     | Page + assets fetched and asserted over HTTPS in the VM test (`tests/pbx.nix:74-76`)                                                      |
| SIP.js IIFE bundle (no CDN, no runtime fetch)        | 🟢 `FULLY_FUNCTIONAL`     | esbuild-bundled from the pinned npm tarball (`packages/webphone/default.nix:29-34`); built as a flake check; MIT notice shipped as `sip.min.js.LEGAL.txt` (`default.nix:44`) |
| Webphone UI (login/register, dial, incoming, mute)   | 🟡 `PARTIALLY_FUNCTIONAL` | SIP.js 0.21.2 API surface validated in node (`packages/webphone/assets/app.js`), but no real browser/WebRTC media call has ever been placed; single-session by design (2nd incoming call is rejected, `app.js:130-134`) |
| `wss://<host>/sip` proxy to FreeSWITCH               | 🟡 `PARTIALLY_FUNCTIONAL` | Proxy reachability asserted indirectly — response is not 502/504 (`tests/pbx.nix:83-84`); the actual WebSocket upgrade is untested          |

## Packaging & flake tooling

| Feature                                              | Status                | Notes                                                                                          |
| ---------------------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------- |
| `webphone` package                                   | 🟢 `FULLY_FUNCTIONAL` | `flake.nix:61`; built by `checks.webphone`                                                     |
| `freeswitch-sounds` package (callie + MOH, 8 kHz)    | 🟢 `FULLY_FUNCTIONAL` | `packages/sounds.nix`; pinned hashes; default for `sounds.package`                              |
| Demo VM (`nix run .#vm`)                             | 🟢 `FULLY_FUNCTIONAL` | `flake.nix:65-70`; same module stack is VM-tested; host evaluates and builds                    |
| Flake checks (VM test, format, statix, deadnix)      | 🟢 `FULLY_FUNCTIONAL` | `flake.nix:72-96`; `nix flake check` passes end to end                                          |
| Dev shell (treefmt wrapper, nil, jq)                 | 🟢 `FULLY_FUNCTIONAL` | `flake.nix:98-104`                                                                              |
| CI workflow (`nix flake check` on GitHub Actions)   | 🟢 `FULLY_FUNCTIONAL`     | `.github/workflows/ci.yml`; verified green end to end twice on GitHub incl. the VM test (`docs/status/2026-08-21_09-40_public-github-release.md` §5) |

## Planned (documented, no code yet)

| Feature                                     | Status               | Notes                                                                        |
| ------------------------------------------- | -------------------- | ---------------------------------------------------------------------------- |
| Secrets via secret manager (sops-nix/agenix) | ⚪ `PLANNED`         | README Security section documents the store-exposure trade-off; no code      |
| `tls.mode = "acme"` auto-wiring              | ⚪ `PLANNED`         | Enum only has `self-signed` / `manual` (`modules/telephony.nix:329-333`)     |
| Multiple ITSP gateways / least-cost routing  | ⚪ `PLANNED`         | Gateway is a single `nullOr submodule` (`modules/telephony.nix:252-259`)     |
| Recordings browsing over nginx               | ⚪ `PLANNED`         | Files land on disk only; no `location /recordings`                           |
| Call detail records (CDR)                    | 🟡 `PARTIALLY_FUNCTIONAL` | `mod_cdr_csv` is loaded (`modules/freeswitch.nix:188`) but left on vanilla defaults — no rotation/sink config |
| Browser E2E test (chromium, fake media)      | ⚪ `PLANNED`         | Awaiting decision on CI cost (ROADMAP open questions)                        |
