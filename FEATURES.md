# Features

> Honest inventory of what this project does, by status. Generated by studying
> the actual code, not the marketing claims. Updated as features ship, change,
> or break.

## Status legend

| Status                    | Meaning                                                      |
| ------------------------- | ------------------------------------------------------------ |
| 🟢 `FULLY_FUNCTIONAL`     | Works as intended, exercised by tests or daily use.          |
| 🟡 `PARTIALLY_FUNCTIONAL` | Ships but has known gaps, edge-case bugs, or missing pieces. |
| 🔴 `BROKEN`               | Present in code but not working / disabled / failing.        |
| ⚪ `PLANNED`              | Designed or documented but **not yet implemented** in code.  |

> A feature earns `FULLY_FUNCTIONAL` only when you can point to the code that
> delivers it AND confirm it works. If you're unsure, it's `PARTIALLY_FUNCTIONAL`
> at best, never round up.

## NixOS module (`services.telephony`)

| Feature                                                         | Status                    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Declarative option schema with assertions                       | 🟢 `FULLY_FUNCTIONAL`     | All options typed + described; 8 assertions (all `services.telephony.*` options); `nix flake check` green                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Declarative IVR menus (`ivrs.<name>`)                           | 🟢 `FULLY_FUNCTIONAL`     | Dial the extension, hear the greeting, press a key: built on mod_dptools' `ivr` menu app with definitions generated into `ivr.conf.xml` (mod_dialplan_xml's parse-before-execute semantics make play_and_get_digits + nested-condition routing impossible — source-verified). VM-tested with real RFC 4733 DTMF: key 1 transfers to the echo test with real streamed audio, key 2 to a ring group's voicemail fallback, `*` and multi-digit keys route, a wrong key exhausts retries into the fallback/hangup (`tests/ivr.nix`) |
| Conference rooms (`conferences.<name>`)                         | 🟢 `FULLY_FUNCTIONAL`     | mod_conference rooms with optional pin; two scripted legs join concurrently, fs_cli sees both members and each receives the mixed bridge audio (byte-asserted on both legs; `tests/conference.nix`). The vanilla `hangup`-on-`#` caller-control is stripped from the generated `conference.conf.xml` (pin + `#` joins instead of instantly expelling — mod_conference stops reading pin digits at the pin length) and prompts resolve via the flattened `freeswitch-conference-sounds-8000-flattened` tree (no plain `sound_prefix` can bridge vanilla's rate-shipped layout); wrong-pin rejection and stay-joined right-pin legs are VM-asserted |
| Time-based ring-group routing (`timeWindow`)                    | 🟢 `FULLY_FUNCTIONAL`     | FreeSWITCH date-time condition attributes (wday/hour; semantics verified against switch_xml_std_datetime_check): in-window the group rings, out-of-window transfers to `afterHoursDestination`. VM-tested by setting the VM clock into and out of the window (`tests/time-routing.nix`)                                                                                                                                                                                                                                         |
| Health monitoring (`monitoring.enable`)                         | 🟢 `FULLY_FUNCTIONAL`     | Timer-driven check unit (loopback-scoped network) with bounded fs_cli retries: fails loudly naming the sick component — event socket dead, profile down, gateway not REGED. VM-tested for all three (`tests/monitoring.nix`)                                                                                                                                                                                                                                                                                                    |
| Off-host backups (`backups.*`, restic)                          | 🟢 `FULLY_FUNCTIONAL`     | `repository`/`repositoryFile` + `passwordFile` (exactly-one-of asserted), `paths`, `calendar`, `pruneOpts`; delegates to NixOS' restic module (`initialize = true`, `Persistent` timer). VM-proven round-trip: canary file → snapshot → `restic ls latest`; default path is the real `/var/lib/private/freeswitch` (restic archives the symlink as a link — caught by the test) (`tests/backup.nix`)                                                                                                                            |
| Failure alerting (`alerts.*`, OnFailure webhook)                | 🟢 `FULLY_FUNCTIONAL`     | `url`/`urlFile` pair (exactly-one-of asserted); a hardened `telephony-alert@%N` template unit POSTs the failed unit's name, host, time and journal tail (`%N`, not `%n` — systemd's specifier expansion doubles the `.service` suffix otherwise). Wired onto the backup, health-check and fail2ban units; VM-proven with a real HTTP sink receiving a real `telephony-health` failure (`tests/backup.nix`)                                                                                                                      |
| fail2ban SIP jail (`fail2ban.enable`)                           | 🟢 `FULLY_FUNCTIONAL`     | Watches the FreeSWITCH journal with a source-verified failregex (`SIP auth failure ... from ip X`; the normal first-contact `auth challenge` line deliberately not counted) and bans repeat sources. VM-tested: bad REGISTERs from a dedicated IP end banned, others untouched (`tests/fail2ban.nix`)                                                                                                                                                                                                                           |
| FreeSWITCH wiring via generated `configDir`                     | 🟢 `FULLY_FUNCTIONAL`     | `services.freeswitch.configDir` wiring; VM test boots the full stack (`tests/pbx.nix`)                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Event socket (`fs_cli`, localhost-only, configured password)    | 🟢 `FULLY_FUNCTIONAL`     | `eventSocketPassword` option or `eventSocketPasswordFile` (runtime-spliced); every VM test assertion drives it                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Echo test extension `9196`                                      | 🟢 `FULLY_FUNCTIONAL`     | generated `dialplan/default.xml`; real `originate loopback/9196` call answered (`tests/pbx.nix`)                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Self-signed TLS bootstrap (per-host cert, nginx-compatible key) | 🟢 `FULLY_FUNCTIONAL`     | `tls.mode = "self-signed"` wiring; exercised by the VM test (nginx serves HTTPS with the generated cert)                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `config.js` generation (SIP domain + TURN ICE servers)          | 🟢 `FULLY_FUNCTIONAL`     | `modules/telephony/web.nix`; body is strict JSON after the JS wrapper, parsed and TURN creds asserted over HTTPS in the VM test; the TURN secret can come from `turn.authSecretFile` (read at render time)                                                                                                                                                                                                                                                                                                                      |
| Inbound ITSP ACL (`gateway.allowedCidrs`)                       | 🟢 `FULLY_FUNCTIONAL`     | Emits `acl.conf.xml` + wires `apply-inbound-acl` on the external profile; an INVITE from a non-listed source on 5080 is rejected before the dialplan (`tests/pbx.nix`)                                                                                                                                                                                                                                                                                                                                                          |
| Firewall management (SIP/RTP/HTTPS/TURN)                        | 🟢 `FULLY_FUNCTIONAL`     | `openFirewall` option; ports reachable in the VM test with `firewall.enable = true`                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Extensions (SIP users: password, display name, toll allow, VM)  | 🟢 `FULLY_FUNCTIONAL`     | REGISTER over TCP with digest auth, wrong-password rejection and multi-device coexistence asserted; registered INVITE answered end to end (`tests/pbx.nix`, `tests/sip.py`)                                                                                                                                                                                                                                                                                                                                                     |
| Ring groups (simultaneous ring, timeout, voicemail fallback)    | 🟢 `FULLY_FUNCTIONAL`     | A real SIP call to the group rings, falls through to the voicemail fallback after the group timeout and is answered by it (`tests/pbx.nix`)                                                                                                                                                                                                                                                                                                                                                                                     |
| ITSP gateways (multi-trunk, LCR, DID inbound, ACL)              | 🟡 `PARTIALLY_FUNCTIONAL` | Two-gateway VM test: REG states, sequential failover order in the generated bridge, per-gateway DID routing to extensions or ring groups (`ringGroupDidEval` eval regression; 404 for unknown DIDs), toll_allow denial, inbound ACL; never validated against a real ITSP                                                                                                                                                                                                                                                        |
| PSTN denial paths (403 without toll allow, 503 without gateway) | 🟢 `FULLY_FUNCTIONAL`     | Scripted SIP INVITEs assert: toll denial answers 603 (hangup `call_rejected`), no-gateway E.164 answers 503, unknown numbers answer 404 (`tests/pbx.nix`)                                                                                                                                                                                                                                                                                                                                                                       |
| Voicemail (per-extension boxes, `*98` check)                    | 🟢 `FULLY_FUNCTIONAL`     | Deposit AND retrieval scripted with a real SIP+RTP+DTMF client (`tests/vmclient.py`, digits as RFC 4733 telephone-event RTP — sofia silently drops `Signal:`-style INFO dtmf-relay): a ring-group timeout falls through to voicemail, >= min-record-len seconds of audio land as `msg_*.wav`, `*98` login (mailbox ID, then PIN) auto-plays messages (asserted as real RTP bytes), a wrong PIN never reaches the messages (`tests/voicemail.nix`)                                                                               |
| Voicemail-to-email (`vmEmail` + `voicemail.mailerCommand`)      | 🟢 `FULLY_FUNCTIONAL`     | `mailerCommand` wires the core mailer-app (with the /bin/cat tmpfiles symlink NixOS needs); `vmEmail` emits vm-mailto + vm-email-all-messages + vm-attach-file (vm-mailto alone never sends — source-verified). VM-tested with a catch-all mailer: the deposited message arrives addressed to the recipient (`tests/voicemail.nix`)                                                                                                                                                                                             |
| Per-extension extras: `callerIdNumber`, `*97` no-record dial    | 🟢 `FULLY_FUNCTIONAL`     | `callerIdNumber` overrides the gateway DID on the PSTN leg (eval-verified), `*97<ext>`/`*97<group>` dial with per-call recording skipped — VM-proven: the destinations route and no recording file appears (`tests/pbx.nix`)                                                                                                                                                                                                                                                                                                    |
| Call recording (`record_session` WAV)                           | 🟢 `FULLY_FUNCTIONAL`     | A dialled call grows a `*_1001.wav` under `/var/lib/telephony/recordings` (shared dir, root:telephony 2770); with `recording.enable = false` no directory is provisioned (`tests/pbx.nix`)                                                                                                                                                                                                                                                                                                                                      |
| NAT advertisement (`natAddress`)                                | 🟡 `PARTIALLY_FUNCTIONAL` | Wired into SDP/SIP vars (`natAddress` option → `ext-sip-ip`/`ext-rtp-ip`); untested behind real NAT                                                                                                                                                                                                                                                                                                                                                                                                                             |
| RTP port range options                                          | 🟡 `PARTIALLY_FUNCTIONAL` | Applied to `switch.conf.xml` + firewall (`rtp.startPort`/`rtp.endPort` → `switch.conf.xml` + firewall); range never asserted                                                                                                                                                                                                                                                                                                                                                                                                    |
| Manual TLS mode (external cert/key paths)                       | 🟡 `PARTIALLY_FUNCTIONAL` | Option + wiring exist; full-system evaluation asserted by `checks.telephony-eval`, never exercised by a runtime test                                                                                                                                                                                                                                                                                                                                                                                                            |
| ACME TLS mode (nginx + FreeSWITCH 5061)                         | 🟡 `PARTIALLY_FUNCTIONAL` | `tls.mode = "acme"` delegates to the nginx vhost's `enableACME` and provisions agent.pem/cafile.pem to the internal profile via a oneshot + renewal path unit; full-system evaluation asserted by `checks.telephony-eval` (which caught and fixed a missing HTTP-01 challenge provider); real ACME needs a public host                                                                                                                                                                                                          |
| coturn STUN/TURN wiring (REST auth)                             | 🟢 `FULLY_FUNCTIONAL`     | STUN binding, TURN allocation with ephemeral credentials (derived from the secret and cross-checked) and wrong-credential 401 all asserted in the VM test (`tests/turn.py`); the secret can come from `turn.authSecretFile` via coturn's native `static-auth-secret-file`                                                                                                                                                                                                                                                       |
| File-based secrets (`*File` options, store purity)              | 🟢 `FULLY_FUNCTIONAL`     | `passwordFile` per extension/gateway, `eventSocketPasswordFile`, `turn.authSecretFile`; generated XML/config.js carries `@TELEPHONY_*@` placeholders, the freeswitch unit splices real values from `LoadCredential` files into a private `/var/lib/freeswitch/conf` copy; exactly-one-of enforced by assertions; `telephony-secrets` VM test proves store purity, runtime modes, mixed plain/file REGISTERs and a live gateway REG state machine off the spliced external profile                                               |
| WebRTC signaling path (wss proxy -> sofia wss-binding)          | 🟢 `FULLY_FUNCTIONAL`     | nginx terminates the browser's wss and re-origins TLS to sofia's `wss-binding` on 127.0.0.1:7443 (`= /sip` exact-match location); two real browsers register and complete a WebRTC call over it (`legacyPackages.telephony-browser`); Via/WS control probes in `tests/wsprobe.py` verify the transport consistency FreeSWITCH enforces                                                                                                                                                                                          |
| WebRTC media (ICE/DTLS-SRTP)                                    | 🟢 `FULLY_FUNCTIONAL`     | `apply-candidate-acl localnet.auto` on the internal profile (the wan.auto default denies private candidates and rejects LAN browsers with 488); a real 1000→1001 chromium call with DTLS-SRTP is bridged and asserted server-side while up (`legacyPackages.telephony-browser`)                                                                                                                                                                                                                                                 |
| aarch64-linux outputs                                           | 🟡 `PARTIALLY_FUNCTIONAL` | Declared (flake `systems` list); `webphone` + `freeswitch-sounds` cross-build and the full demo-VM configuration evaluates to a valid aarch64 toplevel. GitHub arm runners expose no /dev/kvm and the test driver's fixed 300s serial-shell window rules out the full VM suites under TCG; the minimal `telephony-boot-tcg` suite (kernel -> systemd -> freeswitch -> sofia) runs same-arch TCG in CI                                                                                                                           |
| Operator tooling baseline (`opsTools.enable`)                   | 🟢 `FULLY_FUNCTIONAL`     | btop, htop, dig, tcpdump, jq, lsof, sqlite, tmux, vim, openssl on the host shell plus a flake nix CLI (nix-command + flakes) with the nixpkgs registry entry pinned to the exact nixpkgs source the system was built from; tool presence and offline `nix flake metadata nixpkgs` resolution asserted in `tests/pbx.nix`                                                                                                                                                                                                        |
| Operator window + read-model API (`operator.*`, `phoneApi`)     | 🟢 `FULLY_FUNCTIONAL`     | A hardened loopback API reads FS state through a ro bind (`telephony-fs-state-acl` grants the telephony group POSIX-ACL read; the API keeps its own ephemeral uid, cannot write): per-extension phone API (voicemail summary/list/stream/delete with HMAC stream tokens + expiry, CDR history filtered to the authenticated accountcode) behind the extension's SIP credentials, plus the nginx basic-auth operator surface (health cards: sofia profiles/units/cert, CDR viewer, SMS inbox, dialplan dry-run simulator). VM-proven end to end incl. 401 denials and 404-on-foreign-mailbox (`tests/operator.nix`) |
| Inbound fax receive (`faxExtension`, mod_spandsp)               | 🟢 `FULLY_FUNCTIONAL`     | rxfax on the fax extension with T.38 disabled (Telnyx trunk posture); TIFF lands under the fax dir. VM-tested: G.711 call answered, `rxfax(` EXECUTE markers, `fax_use_t38=false`, `NORMAL_CLEARING` hangup (`tests/fax.nix`)                                                                                                                                                                                                                                                                                                |

## Web client

| Feature                                                          | Status                | Notes                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HTTPS webphone serving (static site over TLS)                    | 🟢 `FULLY_FUNCTIONAL` | Page + assets fetched and asserted over HTTPS in the VM test (`tests/pbx.nix`)                                                                                                                                                                                                                                                                                   |
| SIP.js IIFE bundle (no CDN, no runtime fetch)                    | 🟢 `FULLY_FUNCTIONAL` | esbuild-bundled from the pinned npm tarball (pinned tarball + esbuild in the `webphone` package); built as a flake check; MIT notice shipped as `sip.min.js.LEGAL.txt` (`sip.min.js.LEGAL.txt`)                                                                                                                                                                  |
| Webphone UI (login, dial, multi-call hold/switch, DTMF, history) | 🟢 `FULLY_FUNCTIONAL` | Multiple concurrent calls with hold/focus, dtmf-relay keypad, call history + timer, ringback, transport auto-reconnect with re-register and remember-me (extension only); two real chromium browsers register and complete a 1000→1001 WebRTC call through the full stack (TLS, CSP, config.js, wss proxy, ICE, DTLS-SRTP) in `legacyPackages.telephony-browser` |
| Webphone call transfer (blind + attended)                       | 🟢 `FULLY_FUNCTIONAL`     | Blind (REFER) + attended transfer UI on the in-call panel; E2E-proven in the browser suite: a transferred leg re-invites and the new callee answers (`legacyPackages.telephony-browser`)                                                                                                                                                                                                                                                                                                                                      |
| Webphone incoming-call UX                                       | 🟢 `FULLY_FUNCTIONAL`     | Notifications, ringtone, tab title flash on inbound calls; browser E2E asserts the ring state on the callee side (`legacyPackages.telephony-browser`)                                                                                                                                                                                                                                                                                                                                                                        |
| Webphone voicemail panel (list/play/delete, MWI badge)          | 🟢 `FULLY_FUNCTIONAL`     | Drives the per-extension phone API: summary badge, message list, token-authed in-browser playback, delete; server side fully exercised by `tests/operator.nix` (deposit → summary unread → messages → WAV stream → DELETE → zero)                                                                                                                                                                                                                            |
| Webphone contacts + CDR-backed history                          | 🟢 `FULLY_FUNCTIONAL`     | History from the phone API's own-accountcode CDR feed (server filter verified: 1001 sees the deposit call, 1000 sees none); contacts panel rendered client-side                                                                                                                                                                                                                                                                                               |
| Webphone ICE/TURN diagnostics panel                             | 🟢 `FULLY_FUNCTIONAL`     | Live ICE candidate gathering + TURN credential fetch diagnostics in the webphone UI; NAT/ICE is the stack's #1 failure mode (AGENTS.md), now visible in-browser (`legacyPackages.telephony-browser`)                                                                                                                                                                                                                                                          |
| `wss://<host>/sip` proxy to FreeSWITCH                           | 🟢 `FULLY_FUNCTIONAL` | Exact-match `= /sip` location proxying TLS to sofia's `wss-binding`; real WebSocket upgrades carry REGISTER/INVITE traffic in the browser E2E (and the former `/sip` prefix location bug — capturing `/sip.min.js` — is regression-guarded by a bundle fetch assert in `telephony-webphone`)                                                                     |

## Packaging & flake tooling

| Feature                                           | Status                    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `webphone` package                                | 🟢 `FULLY_FUNCTIONAL`     | `packages.webphone`; built by `checks.webphone`                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `freeswitch-sounds` package (callie + MOH, 8 kHz) | 🟢 `FULLY_FUNCTIONAL`     | `packages/sounds.nix`; pinned hashes; default for `sounds.package`                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Demo VM (`nix run .#vm`)                          | 🟢 `FULLY_FUNCTIONAL`     | `apps.vm`; headless (console on stdio), webphone forwarded to `https://localhost:8443/` + SSH on 2222; same module stack is VM-tested; host evaluates and builds                                                                                                                                                                                                                                                                                                                                            |
| Flake checks (VM test, format, statix, deadnix)   | 🟢 `FULLY_FUNCTIONAL`     | flake `checks` incl. the eval-only `telephony-eval` regression check (TLS modes + dial-string escaping, no VM boot); `nix flake check` passes end to end                                                                                                                                                                                                                                                                                                                                                    |
| Initrd driver audit (`initrd-audit`)              | 🟢 `FULLY_FUNCTIONAL`     | `packages/initrd-audit` decompresses zstd/gzip/xz/lz4 initrds and asserts the platform's storage bus drivers; CI gate `checks.initrd-audit` runs it over `pbx-prod`'s real initrd (x86_64) and `docs/deploy.md` §4 makes it a pre-install step — the check that would have caught the unbootable 2026-09-14 first install                                                                                                                                                                                   |
| Metal-path boot proof (`telephony-metal-boot`)    | 🟢 `FULLY_FUNCTIONAL`     | End-to-end companion to initrd-audit (x86_64): partitions a GPT `disk-main-root` on a second disk behind a `virtio-scsi-pci` HBA (Hetzner's bus — NOT the framework's `diskInterface = "scsi"` lsi53c895a), populates it with the pbx-prod closure, and kexec's into the real pbx-prod kernel+initrd with the prod cmdline; console-asserted that the initrd binds the HBA, mounts the by-partlabel root (the 2026-09-14 hang point) and stage-2 systemd boots to the login prompt (`tests/metal-boot.nix`) |
| Per-component VM test suites                      | 🟢 `FULLY_FUNCTIONAL`     | `telephony-dialplan` / `telephony-webphone` / `telephony-tls-turn` / `telephony-secrets` / `telephony-boot` / `telephony-ssh` single-node checks on shared `tests/common.nix` fixtures, plus the multi-node `telephony` integration check — run one suite for fast bisect (`nix build .#checks.x86_64-linux.telephony-dialplan`)                                                                                                                                                                            |
| Browser E2E suite (chromium, fake media)          | 🟢 `FULLY_FUNCTIONAL`     | `legacyPackages.telephony-browser` (deliberately outside `checks`: ~1-2 GB chromium closure): two headless chromiums register over wss and complete a real WebRTC call, asserted server-side via fs_cli; self-diagnosing failure dumps (browser console, webphone event log, chromedriver logs, raw WS probes)                                                                                                                                                                                              |
| Dev shell (treefmt wrapper, nil, jq)              | 🟢 `FULLY_FUNCTIONAL`     | flake `devShells`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Pre-commit hooks (git-hooks.nix)                  | 🟢 `FULLY_FUNCTIONAL`     | nixfmt + statix + deadnix + gitleaks (custom-wrapped) + changelog-headings (Keep-a-Changelog decay) + scrub-check (personal-data patterns from the gitignored `secrets/scrub-patterns.txt`) installed by `nix develop`; `pre-commit run --all-files` clean over the tree                                                                                                                                                                                                                                    |
| CI workflow (`nix flake check` on GitHub Actions) | 🟢 `FULLY_FUNCTIONAL`     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Monthly flake-update workflow                     | 🟢 `FULLY_FUNCTIONAL`     | `.github/workflows/flake-update.yml`: scheduled `nix flake update` + cross-arch eval gate, opens a reviewable refresh PR                                                                                                                                                                                                                                                                                                                                                                                    |
| TURN over TLS (`turn.tls`)                        | 🟡 `PARTIALLY_FUNCTIONAL` | Option wires coturn's turns:/DTLS listener + firewall (eval-verified); not exercised by a runtime test (needs a real cert setup)                                                                                                                                                                                                                                                                                                                                                                            |

## Host integration (example hosts)

| Feature                                     | Status                    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Hardened SSH server (nix-ssh-config)        | 🟢 `FULLY_FUNCTIONAL`     | `nix-ssh-config` input (`nixpkgs.follows`) wires `nixosModules.ssh` into `nixosConfigurations.pbx` with the tracked `sshKeys` authorized, pinned to upstream `v0.1.3` where keys-only finally means keys-only (kbd-interactive follows `passwordAuthentication`; the old downstream workaround is retired; v0.1.3 adds VM runtime proofs, a golden `sshd -T` snapshot and new options incl. `listenAddresses`/`usePam`/`authenticationMethods`); `checks.telephony-ssh` (`tests/ssh.nix`) asserts the effective sshd config (keys-only incl. kbd-interactive off, no root login, `mlkem768x25519-sha256` kex first, AEAD-only ciphers, forwarding off, banner), a real key-based login negotiating the ML-KEM hybrid kex, and the password/root denial paths; demo VM forwards host 2222→22 and relaxes to `allowRootLogin = true` (documented demo convenience)                                                                                                                                                                                                                                                                                                                                                 |
| Production host template (`hosts/pbx-prod`) | 🟡 `PARTIALLY_FUNCTIONAL` | `nixosConfigurations.pbx-prod`: real disk layout (disko — ext4 on `/dev/sda`, GPT + BIOS-boot) with explicit virtio initrd modules (the metal boot path VM tests cannot cover), `*File` secrets only (single `secretsDir` knob, persisted under `/var/lib/telephony-secrets`), `tls.mode = "acme"`, CDR, commented gateway + `allowedCidrs`/`restrictExternalTo` posture, keys-only root SSH for the documented `nixos-rebuild --target-host` updates (`allowRootLogin = true` ≡ prohibit-password, `allowUsers = [ "root" ]`), backups + failure alerting enabled against `CHANGEME`-marked secret files, neutral example values (real deployments live in a private flake consuming `nixosModules.telephony`); full toplevel evaluation is forced by `nix flake check` (verified: acme terms, vhost, cdr wiring), and the template's unit graph is boot-proven by `checks.telephony-prod-boot` (stubbed secrets + self-signed TLS on the real shape: sofia bound non-loopback, nginx serving over TLS, spliced secrets authenticate a REGISTER, nothing `CHANGEME`-shaped in runtime config). Runtime on real hardware is not yet exercised — awaits the first actual deployment (`docs/deploy.md`, TODO_LIST) |

## Planned (documented, no code yet)

| Feature                     | Status       | Notes                                                                                                  |
| --------------------------- | ------------ | ------------------------------------------------------------------------------------------------------ |
| Wired sops-nix example host | ⚪ `PLANNED` | Docs-only recipe shipped (`docs/secrets.md`); wiring sops-nix into `hosts/pbx` awaits the owner's call |

## Operations

| Feature                                                     | Status                | Notes                                                                                                                                                                                                                                                                     |
| ----------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Call detail records (`cdr.enable`, CSV)                     | 🟢 `FULLY_FUNCTIONAL` | One row per call leg appended to `/var/lib/freeswitch/cdr-csv/Master.csv`; VM test asserts a row after a finished call                                                                                                                                                    |
| Recordings browsing (`recording.serve`, nginx + basic auth) | 🟢 `FULLY_FUNCTIONAL` | `/recordings/` answers 401 without (or with wrong) credentials and lists the WAV with correct ones; htpasswd rendered at runtime from `basicAuthPasswordFile` (`tests/pbx.nix`)                                                                                           |
| Recordings retention (`recording.retentionDays`)            | 🟢 `FULLY_FUNCTIONAL` | Daily timer deletes WAVs past the window; VM test prunes a 30-day-old file and keeps a fresh one (`tests/pbx.nix`)                                                                                                                                                        |
| `extraConfigFiles` escape hatch                             | 🟢 `FULLY_FUNCTIONAL` | attrsOf path merged into the generated config (operator files override on key collision); VM test asserts an extra dialplan file lands verbatim in the store config dir; key-safety (no `/`, `..`, whitespace) enforced by assertion                                      |
| systemd hardening (telephony units)                         | 🟢 `FULLY_FUNCTIONAL` | Root oneshots run `ProtectSystem=strict` + `NoNewPrivileges` + RAF-limited with tmpfiles-precreated writable state; freeswitch gets `NoNewPrivileges`/`ProtectHome`/RAF incl. `AF_NETLINK` (getifaddrs for NAT detection — the first INVITE stalls without it, VM-tested) |
| Ops runbook                                                 | 🟢 `FULLY_FUNCTIONAL` | `docs/ops-runbook.md`: service inventory, fs_cli cheat-sheet, health checks (incl. wss/7443 + listening-port table), cert rotation per `tls.mode`, gateway REG-state table, recordings/TURN rotation, emergency actions; architecture diagram in README                   |
| sops-nix secret recipe                                      | 🟢 `FULLY_FUNCTIONAL` | `docs/secrets.md`: age key setup, `.sops.yaml`, `sops.secrets` shape incl. `owner = "turnserver"` for coturn, verification steps; option defaults verified against the sops-nix module source; the VM secrets test models the exact rendered shape                        |
