# TODO List

> Short-term, actionable, bounded work items, verified against the actual code.
> For long-term vision and unrefined ideas, use ROADMAP.md.
> Items are ranked by impact. Status is verified, not assumed.

## Status legend

| Status           | Meaning                                                     |
| ---------------- | ----------------------------------------------------------- |
| 🔴 `TODO`        | Not started. Needs doing.                                   |
| 🟡 `IN_PROGRESS` | Actively being worked on.                                   |
| 🔵 `BLOCKED`     | Cannot proceed, external dependency or decision needed.     |
| 🟢 `DONE`        | Completed. Remove from this list and log in `CHANGELOG.md`. |

## High Impact

| Task                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Status       | Impact   | Effort | Evidence                                                                                                                                                                                                                                                                                                              |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | -------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| First real deployment, P1–P5 lane: rescue-boot the recreated server, rerun the fixed install, verify the live stack, first calls. User steps: Hetzner rescue enable + power-cycle, rerun `install-pbx.sh` + `push-secrets.sh` from evo-x2; then verify the webhook health endpoint, PATCH the messaging-profile webhook URL, close the outbound-call loop, re-add static IPv6 + AAAA, delete the old (billing) server, run the docs/deploy.md §5 checklist, first real calls + CDR rows, then trunk hardening (Telnyx source CIDRs + fail2ban) and the live security pass (Hetzner Cloud Firewall apply + one `ssh-audit` run; `docs/security.md` going-live checklist) | 🔵 `BLOCKED` | Critical | 2h     | Repo side done and metal-boot-proven; owner decisions recorded 2026-09-16 (record ALL calls by default, consent accepted — ROADMAP open question 5). Remaining blockers are the listed user hands-on steps only; the private flake, webhook receiver and runbook are ready (docs/deploy.md §3–5, docs/ops-runbook.md) |

## Blocked (owner)

| Task                                                                                                                                                                                                                                                                                                                               | Status       | Impact | Effort | Evidence                                                                                                                                                                                                                    |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ------ | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cut v0.3.0: date `[Unreleased]`, tag, `gh release create`, repo metadata refresh. The section keeps growing (operator API + window, conference `#` user-visible behavior change, fax suite, opsTools baseline, scrub-gate labels, webphone extraction) and the release was originally gated on the first real call                 | 🔵 `BLOCKED` | High   | 30min  | `CHANGELOG.md [Unreleased]`; release flow in the repo README ("Releases"); deploy timing per the owner                                                                                                                      |
| fspbx trial verdict sign-off, then execute it: kill = revoke the live Sanctum PAT, stop the VM, trash `/var/tmp/fspbx-trial`; keep = relocate to persistent storage + snapshot. Closes the CDR-GUI-render / who-answers-1002 loose ends either way                                                                                 | 🔵 `BLOCKED` | Medium | S      | Recommendation with evidence delivered (retire VM, stay NixOS-first, keep fspbx as feature reference): verdict banner in `docs/research/2026-09-16_fspbx-trial.md`; VM + `pre-sip-wiring` snapshot preserved until sign-off |
| Warsaw DID: re-purchase in the portal + submit the 5 KYC requirements inside the ~48h release window; then DE national DID order. The first Warsaw order was auto-deleted by Telnyx (KYC unsubmitted inside the window)                                                                                                            | 🔵 `BLOCKED` | High   | S      | `docs/providers/telnyx.md` (KYC requirements + lead times); owner deferred until first calls are green                                                                                                                      |
| Rotate the Telnyx API key (transited chat and /tmp; now load-bearing for the CC/outbound-profile scripts); update the `KEY…` prefix pattern in `secrets/scrub-patterns.txt` in the same action                                                                                                                                     | 🔵 `BLOCKED` | Medium | S      | Pattern coupling visible in `secrets/scrub-patterns.txt` (`KEY01…` prefix entry); scrub gate: `scripts/scrub-check.sh`                                                                                                      |
| Fill or delete the three `secrets/scrub-patterns.txt` "OWNER TO ADD" placeholders (German mobile spellings, ssh key fingerprints, the server's IPv6 /64 once pinned) — or declare them out of scope and drop the block                                                                                                             | 🔵 `BLOCKED` | Low    | S      | The placeholder block in the gitignored `secrets/scrub-patterns.txt` (template: `secrets/scrub-patterns.example`)                                                                                                           |
| Promote browser E2E CI from manual dispatch (periodic/per-push)                                                                                                                                                                                                                                                                    | 🔵 `BLOCKED` | Low    | 15min  | `workflow_dispatch` job landed and ran green; periodic/per-push gating is the owner's call (ROADMAP open question 3)                                                                                                        |
| `flake-meta-checker` mainProgram policy for data packages: accept the perpetual info finding vs upstream carve-out (no fake values)                                                                                                                                                                                                | 🔵 `BLOCKED` | Low    | S      | Both packages are data derivations with no executable; accepted remainder recorded in AGENTS.md (BuildFlow-noise paragraph)                                                                                                 |
| Upstream BuildFlow feedback (verify-before-filing first): config keys for max_time/budget; nix-checker FOD-hash advisory is false-positive-by-design; mainProgram impossible for data packages; todo-checker findings should name the matched marker                                                                               | 🔵 `BLOCKED` | Low    | M      | AGENTS.md accepted-remainder paragraph is the local record                                                                                                                                                                  |
| Wire sops-nix into an example host (docs/secrets.md recipe stands; owner-gated)                                                                                                                                                                                                                                                    | 🔵 `BLOCKED` | Low    | S      | FEATURES.md PLANNED row; ROADMAP open question 1 remainder                                                                                                                                                                  |
| GitHub residual-exposure appetite after the history rewrite: ask support to GC cached old commits + audit stale Dependabot PR refs, or accept the documented residual (CHANGELOG Security entry) + natural GC; also inventory pre-rewrite clones on other machines (evo-x2, the private-flake box, laptops still hold old history) | 🔵 `BLOCKED` | Low    | S      | CHANGELOG [Unreleased] Security entry records the residual; the clone inventory is owner-only knowledge                                                                                                                     |
| Push the webphone shared-contacts fix (local commit in the webphone clone at `/tmp/webphone`: `SharedContact` json tags — the wire carried `Name`/`Number` while the island reads `name`/`number`, silently dropping every shared contact), relock this repo's webphone input, then flip the contacts asserts in `tests/webphone.nix` back to lowercase | 🔵 `BLOCKED` | Medium | S      | Fix + upstream regression test committed and green in the clone (full `go test ./...` ok); push is owner-gated; `tests/webphone.nix` pins the capitalized wire keys with a comment until the lock moves            |

## Medium Impact

| Task                                                                                                                                                                                                                                                                                                                                               | Status       | Impact | Effort | Evidence                                                                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Operator security hardening: scope the FS-state ACL to a narrower group than `telephony` (nginx is a member and can read the whole FreeSWITCH state tree incl. `core.db` with SIP credential hashes) or per-file grants, as a deliberate decision; plus a dedicated stream-token secret instead of HMAC-keying stream tokens with the ESL password | 🔵 `BLOCKED` | Medium | M      | The ACL unit ships in `modules/telephony/pbx.nix`; trust-circle posture flagged by the operator-API session (2026-09-17); nginx's recordings access is the exposure to preserve |

## Low Impact

| Task                                                                                                                                                                                                                                                                                                                                                                                                           | Status    | Impact | Effort | Evidence                                                                                                                     |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------ | ------ | ---------------------------------------------------------------------------------------------------------------------------- |
