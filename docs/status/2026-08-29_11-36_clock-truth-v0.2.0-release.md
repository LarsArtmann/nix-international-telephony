# Status Report — 2026-08-29 11:36: The Clock-Truth Session (v0.2.0 shipped)

> Point-in-time snapshot. Scope: **this session only** (resume after the
> 09:55 DTMF-truth session). Annotate, never rewrite, when stale.
> Format note: written as Markdown per explicit user instruction
> (overrides this report's usual HTML rendering).

## Executive summary

The session goal was "finish the gate, ship v0.2.0". The gate exposed a
fifth fabricated green — `telephony-time-routing` had **never genuinely
passed**. Root cause took two design iterations to kill properly
(FreeSWITCH ignores backwards clock jumps; CI runners additionally
revert moved clocks), and the final fix (per-leg QEMU RTC bases) is
green locally, on CI, and inside the full gate. **v0.2.0 is released**
on a fully green tree. One unexplained foreign working-tree change
(`hosts/pbx/default.nix`) appeared mid-session and was deliberately
left untouched.

| Metric | Value |
| --- | --- |
| Commits pushed this session | 2 (`448d4a4`, `506fa82`) |
| Red CI runs shipped | 1 (`448d4a4` — restart design, reverted clock on CI) |
| Fabricated greens caught (cumulative) | 5 (voicemail PIN, IVR, conference, fail2ban, time-routing) |
| Final CI state | x86 ✅ aarch64 ✅ browser E2E ✅ (dispatched, green) |
| Release | v0.2.0 tagged on `506fa82`, GitHub release + 9 repo topics |
| Unresolved foreign change | 1 file (see d.3) |

## a) FULLY DONE

1. **Fail2ban suite verified green** — the prior session's unverified
   `ignoreself = false` fix held (local build, exit 0). The last open
   question from the 09:55 report is closed.
2. **time-routing root-caused with source-level evidence** —
   interactive-driver probe (60s of `fs_cli -x 'strepoch'` /
   `eval ${strftime(...)}` polling) proved FreeSWITCH's
   monotonic-plus-offset clock never follows a backwards `date -s`;
   A/B on parent commit `a116877` + CI logs proved the suite had never
   genuinely passed anywhere (same deterministic failure on 4
   independent runs: local ×2, CI, parent).
3. **time-routing fixed twice, properly the second time**:
   - attempt 1 (`448d4a4`): restart-based `move_clock()` — green
     locally, **failed CI** (guest clock reverted to host time between
     `date -s` and restart; additionally the first fix's journal grep
     was wrong — post-startup evidence only lands in
     `/var/lib/freeswitch/log/freeswitch.log`).
   - attempt 2 (`506fa82`): one node per leg with fixed QEMU RTC base
     (`-rtc base=2026-08-29T03:30:00` / `T12:00:00`), loud
     `date +%H` precondition asserts, file-log greps. Green locally
     twice, on CI, and in the full gate. Vacuous-green-forever impossible:
     the hour assert fails loudly if anything drags the clock.
4. **Full gate green** — `nix fmt -- --fail-on-change` + `nix flake
   check` (all checks incl. the rewritten suite).
5. **Pushed + CI green** — `448d4a4`, then `506fa82`: x86 flake check ✅,
   aarch64 TCG boot ✅. Browser E2E workflow dispatched once → ✅.
6. **v0.2.0 released**: CHANGELOG cut (`[0.2.0] - 2026-08-29` + fresh
   Unreleased), annotated tag on the CI-green commit, GitHub release
   with honest notes, 9 repo topics added (freeswitch, nix, nix-flake,
   nixos, pbx, sip, telephony, voicemail, webrtc), release TODO row
   deleted.
7. **Knowledge captured** — AGENTS.md clock-truth bullet + CHANGELOG
   Fixed entry (both re-aligned to the final RTC design, no split
   brain); the prior session's DTMF status report confirmed tracked.
8. **Foreign change preserved** — an uncommitted
   `hosts/pbx/default.nix` edit (demo VM port-forward 443 →
   `127.0.0.1:8443`, banner updated) appeared mid-session; judged
   sound, unstaged from my commits, left in the working tree.

## b) PARTIALLY DONE

1. **Release-notes claim audit** — the v0.2.0 notes are assembled from
   prior-session summaries (i18n, watchdog, secrets coverage...). The
   claims are backed by those sessions' evidence, but this session only
   personally re-verified: time-routing, fail2ban, the full gate, and
   one browser E2E run. A docs-health VERIFY pass over the release
   notes vs FEATURES.md is outstanding.
2. **TODO/ROADMAP harvest** — section (f) below is the raw fuel; not
   yet routed into TODO_LIST.md (which now holds only blocked rows).
3. **Prior-report annotation** — the 09:55 report's §f items 1–4 are
   now done; it should get an ANNOTATE-mode marker, not a rewrite.

## c) NOT STARTED (deliberately, this session's scope)

- G1 first real deployment (owner-blocked: server, DNS, ITSP, secrets).
- G2 browser-E2E CI cadence decision (owner call: manual vs periodic).
- Watchdog auto-recovery deep-dive (backlog from 09:55 §f).
- Test-infra lint: forbid bare-int `timeout=` and journalctl greps of
  post-startup FreeSWITCH lines in test scripts.
- Release automation (CHANGELOG cut + tag + `gh release` as a flake app).
- Ops-runbook note: real-host clock changes (NTP corrections) require a
  FreeSWITCH restart to be seen by date-time routing.

## d) TOTALLY FUCKED UP (honest list)

1. **I shipped a red CI run.** `448d4a4` (restart design) was pushed on
   one local green whose leg-2 was structurally **unable to
   distinguish** "routed via 12:00" from "routed via reverted host
   time" — both are out-of-window. I even noticed the vacuous-leg
   possibility mid-debugging, then let a local green override the
   skepticism. Cost: one CI round trip + a second design iteration.
   The doctrine "prior greens lie" applies to MY OWN greens too.
2. **Four process fumbles, each a known trap:** probe script written
   before reading `common.nix` (helpers not in scope → NameError); the
   `$?`-after-pipe class AGAIN (mis-indexed PIPESTATUS printed an empty
   FMT_EXIT and I moved on without noticing); `--print-out-path` typo
   (summary had the correct `--print-out-paths`); one edit rejected for
   editing from `cat -n` output instead of View (mtime had moved).
3. **Unexplained working-tree change not escalated.** The
   `hosts/pbx/default.nix` port-forward edit appeared between two of my
   `git status` calls with no commit, no session note, and no mention
   in the handover summary. I preserved it (correct) but only surface
   it now — it should have been question #1 in the session's first
   report, not a footnote in the last one.

## e) WHAT WE SHOULD IMPROVE (self-review answers)

- **Did I lie?** No claim in chat or commits was unverified at
  statement time. Residual honesty risk: release notes inherit
  prior-session claims not re-audited here (see b.1).
- **Ghost systems?** None created. The deleted `move_clock` and the
  old CHANGELOG/AGENTS wording were replaced in the same commits — no
  dangling references (grep-verified mentally across the two docs; both
  now describe the RTC design only).
- **Split brains?** None found: AGENTS.md bullet, CHANGELOG entry, and
  the test file all tell one story. `/tmp/fs-2c1` (prior session's A/B
  worktree) is stale but harmless.
- **Scope creep?** One instance: the RTC redesign touched the test's
  whole shape rather than patching further — justified by two failed
  cheaper designs, and it removed an entire failure class instead of
  one symptom.
- **How to be less stupid (top 3):**
  1. A local green is a hypothesis, not a verdict: for any
     clock/state-sensitive assert, ask "what ELSE would make this
     pass?" before pushing (the leg-2 lesson, generalized).
  2. Encode the session's traps as machine checks instead of memory
     (the journal-grep lint, the bare-int timeout sweep — both below).
  3. Read the fixture file (`common.nix`, helpers) before writing code
     that uses it — the probe NameError was pure laziness.

## f) NEXT (ranked; #1–#3 owner-gated, rest is TODO/ROADMAP fuel)

| # | Task | Impact | Effort | Fuel for |
| --- | --- | --- | --- | --- |
| 1 | Decide on the foreign `hosts/pbx` change: commit or discard | High | 2min | owner |
| 2 | G1: provide deployment inputs (server/DNS/ITSP/secrets) | Critical | 2h+ | TODO (blocked) |
| 3 | G2: pick browser-E2E cadence (manual / nightly / per-push / on-tag) | Med | 15min | TODO (blocked) |
| 4 | docs-health VERIFY: release notes + FEATURES vs actual behavior | High | 1h | TODO |
| 5 | Sweep all test scripts for bare-int `timeout=` (deprecation; time-routing had them until today) | Med | 30min | TODO |
| 6 | Pre-commit lint: no `journalctl -u freeswitch` greps for post-startup evidence (file-log only) | Med | 30min | TODO |
| 7 | Annotate the 09:55 report (§f items now done) | Low | 10min | TODO |
| 8 | HARVEST this section into TODO_LIST/ROADMAP | Med | 20min | TODO |
| 9 | Ops-runbook: clock changes need FS restart (date-time routing) | Med | 15min | TODO |
| 10 | Dedup `call()`/file-log-grep helpers into tests/common.nix (ivr, voicemail, time-routing repeat them) | Med | 45min | TODO |
| 11 | Release automation: flake app cutting CHANGELOG + tag + gh release | Med | 2h | ROADMAP |
| 12 | Watchdog auto-recovery deep-dive (reload-recovery guaranteed; auto path best-effort) | Med | 3h | ROADMAP |
| 13 | Shorten in-window leg hold (12s → ~7s) and watch CI total runtime after the 2-node suite | Low | 15min | TODO |
| 14 | gitleaks rescan post-v0.2.0 commits | Low | 10min | TODO |
| 15 | Consider browser E2E as required check on tag pushes (`workflow_run`) | Med | 30min | ROADMAP |
| 16 | Remove stale `/tmp/fs-2c1` worktree (prior session's) | Low | 2min | TODO |
| 17 | Add `assert_fs_hour`-style helper to common.nix for future time-sensitive tests | Low | 20min | ROADMAP |
| 18 | Check whether aarch64 TCG could ever run time-routing (2 VMs vs 300s connect window) before promoting anything | Low | 30min | ROADMAP |
| 19 | DOMAIN_LANGUAGE: verify entries exist for "time window"/"after-hours destination" | Low | 10min | TODO |
| 20 | FEATURES.md: confirm the time-routing row reflects "genuinely tested as of v0.2.0" | Low | 10min | TODO |
| 21 | Roadmap: post-0.2.0 theme proposal (deployment dogfood vs feature work) for owner | Med | 30min | ROADMAP |
| 22 | CI: job naming/splitting if the 2-node suite pushes the check job past ~8min | Low | 30min | ROADMAP |
| 23 | Consider `-rtc driftfix` audit for long suites (theoretical drift within a run) | Low | 30min | ROADMAP |
| 24 | Add the "5 fabricated greens" pattern as a blurb in README's testing claims (marketing honesty) | Low | 15min | TODO |
| 25 | Time-box a "trust no green" pass: pick 2 older suites (secrets, ssh) and re-run once on CI | Med | 1h | TODO |

## g) QUESTIONS ONLY THE OWNER CAN ANSWER

1. **The `hosts/pbx/default.nix` working-tree change (port-forward to
   `127.0.0.1:8443` + banner): yours? commit or discard?** It is
   currently invisible to every flake build and CI run (untracked
   changes don't exist to Nix; uncommitted modifications are only in
   your working tree).
2. **G1, unchanged from two reports ago:** deployment inputs
   (server/domain/DNS/ITSP choice + real secrets). Everything
   repo-side is built, tested, and released; this is the only gate
   left before the stack runs on real hardware.
3. **G2:** should browser E2E stay manual-dispatch, or become
   nightly / per-push / required-on-tags? (~10 min and a ~1–2 GB
   chromium closure per run.)
