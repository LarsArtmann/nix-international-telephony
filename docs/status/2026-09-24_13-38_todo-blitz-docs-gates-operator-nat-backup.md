# Status Report: TODO-list execution blitz — docs gates, operator API, NAT proof, backup restore (2026-09-24 13:38)

> Scope: this session only (12:04–13:38 CEST). Trigger: "execute the
> TODO list" — every actionable (non-BLOCKED) TODO row was worked to a
> verified-green state; the two red main-branch VM suites found on the
> way were root-caused and repaired; one upstream bug was found, fixed
> and committed (locally, unpushed). Nothing outside this session was
> re-audited.

## a) FULLY DONE (verified green this session)

| # | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Proof                                                                                                                                                                                                                                                                                                                                                                               |
| - | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | **P39 drift_alarm extensions**: two new citation arms — a TODO row citing a `docs/status/`/`docs/planning/` snapshot (archived OR live) fails, and a row citing a repo-relative path missing from the tree fails (the ghost-citation class). 12-case `--self-test` negative-tests every arm incl. controls (absolute paths, globs, gitignored local files, section refs); `checks.docs-drift` rebuilt and runs self-test + gate (third CLI arg is the flake source: a per-file store path lands directly in /nix/store with no tree). The P31/P32 row's live-snapshot citation re-pointed; AGENTS + CHANGELOG updated                                                                                                                                                                                                                                                                                                | `nix build .#checks.x86_64-linux.docs-drift` green; direct run PASS                                                                                                                                                                                                                                                                                                                 |
| 2 | **P38 remainder**: deploy.md §5 human checks gained the operator-window login/health-card probe, phone-API history + voicemail-summary probes (extension SIP creds), and the fax feed-unit/directory probe; ops-runbook gained the extension-password rotation ↔ `AUTH_CACHE_TTL` (300s) interplay note (old password stays valid up to 5 min; restart `telephony-operator` to drop)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | claims grep-verified against api.py/fax-feed units                                                                                                                                                                                                                                                                                                                                  |
| 3 | **Operator API tail** (`packages/telephony-operator/api.py`): CDR pagination (`offset` + `more` past the 500 clamp) with dashboard prev/next paging, CSV export (`/api/cdr.csv`, dashboard link), per-extension brute-force damper (5 failures → 15-min lock, 429 even for correct creds — the lock beats the success cache), single-range HTTP seek on audio (206/416), mark-read/unread via mod_voicemail's `vm_read` (verified against upstream mod_voicemail.c: `vm_read <id>@<domain> <read                                                                                                                                                                                                                                                                                                                                                                                                                     | unread> [<uuid>]`,`+OK`/`-ERR`), voicemail-db-reachability probe in`/healthz`(503 when unreachable). **Security fix included**:`vm_delete`(and the new`vm_read`) delegate with uuid-only SQL scoping in mod_voicemail — the API now verifies the uuid belongs to the authenticated mailbox FIRST (an authenticated extension could previously delete any mailbox's message by uuid) |
| 4 | **Quality-gate findings curation**: all 5 bandit detect-only findings in the operator package suppressed inline with rationale (B404/B607 fixed-argv subprocess; B405/B314 operator-generated dialplan XML) — 0 findings remain; the `AUDIO-DEBUG-TEST` prints in tests/operator.nix annotated as deliberate failure evidence; shellcheck SC1083 batch fixed (`'@{upstream}'` quoted) + SC2164 (`cd … \|\| exit`); **real bug found**: `scripts/ahead-check.sh` fetched the tracking ref (`origin/main`) instead of the remote — every run exited 2 "cannot judge"; now fetches the remote and correctly reports ahead/behind                                                                                                                                                                                                                                                                                        | `nix develop -c bandit -r packages/telephony-operator/` → 0 findings; `buildflow -s shellcheck` → 0 SC findings; both scripts behaviorally re-run                                                                                                                                                                                                                                   |
| 5 | **Backup suite upgrade**: the VM test now proves the RECOVERY path — canary → backup → `restic ls` → delete → `restic restore` returns the exact bytes; SSH host-key identity (`/etc/ssh`) rides in the snapshot. Decision (P27): `/etc/ssh` added to the pbx-prod template's backup paths (regenerated host keys break every client's known_hosts under the keys-only posture); runbook restore-drill gained the host-key step; module default unchanged (telephony module stays host-identity-agnostic)                                                                                                                                                                                                                                                                                                                                                                                                            | `checks.telephony-backup` VM suite green incl. restored `OPENSSH PRIVATE KEY` assert                                                                                                                                                                                                                                                                                                |
| 6 | **NAT runtime suite (NEW)**: `tests/nat.nix` + `checks.telephony-nat` — two-NIC VM topology (port-forwarding router node, framework vlans with auto 192.168.x.y addressing, DNAT 5060 tcp+udp; PBX behind it with `natAddress` = router's outer IP). A scripted caller on the OUTER network REGISTERs and completes a real call to 9196 through the forward (ACK/BYE riding the advertised Contact is the behavioral proof); `tests/sip.py` gained `--dump-dialog`; asserts: natAddress in Contact + SDP `c=`, Via present-and-intact, zero private-address leak. **Finding**: the original "Via advertises natAddress" expectation was wrong for this leg — a UAS 200 OK echoes the CALLER's Via set (RFC 3261 §8.2.6); FS appends none on a leg it terminates (empirically confirmed; the failure output showed only the caller's Via). FEATURES `natAddress` row promoted PARTIALLY_FUNCTIONAL → FULLY_FUNCTIONAL | `nix build .#checks.x86_64-linux.telephony-nat` exit 0                                                                                                                                                                                                                                                                                                                              |
| 7 | **Two red main-branch suites repaired** (both red since the 2026-09-23 config.js switchover, root-caused not masked): tls-turn asserted the removed nginx shadow file `/var/lib/telephony/config.js` and never waited for the app that serves `/config.js` now; webphone grepped `'"phoneApi": false'` against the app's now-MINIFIED JSON (`"phoneApi":false`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | both checks rebuilt green                                                                                                                                                                                                                                                                                                                                                           |
| 8 | **Upstream webphone bug — fix prepared (NOT pushed)**: the served config.js emits shared contacts with `Name`/`Number` keys (upstream `SharedContact` struct carries no json tags) while the island's typeahead + contacts panel read lowercase `name`/`number` — every operator-provided shared contact is silently dropped client-side. Fix (json tags) + regression test committed in the webphone clone at `/tmp/webphone` (`31a8604`), full `go test ./...` green; stack-side tests pin the CURRENT capitalized wire shape with an explanatory comment; TODO row added (BLOCKED on owner push + relock + assert flip)                                                                                                                                                                                                                                                                                           | clone's test run output; `checks.telephony-webphone` green against pinned shape                                                                                                                                                                                                                                                                                                     |
| 9 | **Hygiene probes (P31/P32)**: 15/15 round-2-cited hashes resolve (0 dangling); doctor's unavailable-tool list reconciled = tools pinned in the devshell by design (canonical runs belong inside `nix develop`); buildflow rebuilt from the local tree (5b3483a) but the ACTIVE binary is system-profile-pinned (7e1fbfe) — refresh needs a home-manager/NixOS switch (owner, advisory); legacy db VACUUMed (58M, the 2.7GB figure was stale); mypy 2.1.0 green over ALL 10 repo python files (decision: keep all-files coverage, selenium-only import ignore stays); dprint purpose = buildflow's delegated json/yaml/md formatter (keep); **P32 already landed**: the docs-health skill ships `annotate-rows.py`/`annotate-prose.py` + tests + docs (the TODO clause was stale)                                                                                                                                     | command outputs above; skill assets listing                                                                                                                                                                                                                                                                                                                                         |

## b) PARTIAL / not executed (stopped on instruction)

| Item                                                                                                      | State                | Why                                                                                                                                        |
| --------------------------------------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| Canonical `buildflow --build-mode full --max-time 60m` run (P31.4's "1 skipped" confirmation rides on it) | NOT run              | user halt; expected: the 1 skipped = pytest-test via `skip_steps`, gitleaks/codespell/markdown-lint run in full mode                       |
| One-shot full `nix flake check`                                                                           | NOT run this session | user halt; every touched suite verified green individually (operator, backup, nat, tls-turn, webphone, docs-drift, format after `nix fmt`) |

## c) Top next tasks (ranked)

1. Canonical full pipeline: `nix develop -c buildflow --build-mode full --max-time 60m` (pinned binaries; confirms the "1 skipped" step identity), then `nix flake check` for the single all-suites gate.
2. OWNER: push the webphone shared-contacts fix (local commit `31a8604` in /tmp/webphone), relock this repo's webphone input, flip the contacts asserts in `tests/webphone.nix` to lowercase `name`/`number` (TODO row exists).
3. OWNER: buildflow binary refresh via the system profile (advisory; local rebuild already done).

## Notes

- The auto-commit daemon committed throughout; the working tree at halt
  is clean modulo the daemon's lag (operator.js re-formatted by `nix fmt`
  after the daemon's last sweep).
- Session invariants kept: no snapshot rewritten (annotate-only), living
  docs updated in place with CHANGELOG entries, every removed TODO row
  logged in CHANGELOG `[Unreleased]`.
