# The browser E2E (tests/browser-e2e.py) runs inside the NixOS test VM;
# selenium ships no type stubs, so silence only its missing-import errors.
[mypy]

[mypy-selenium.*]
ignore_missing_imports = True
